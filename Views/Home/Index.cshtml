@{
    ViewData["Title"] = "Home Page";
}


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <style>
        * {
            box-sizing: border-box
        }

        body {
            font-family: Verdana, sans-serif;
            margin: 0
        }

        .Images {
            display:;
        }

        img {
            vertical-align: middle;
        }

        /* Slideshow container */
        .slideshow-container {
            max-width: 700px;
            position: relative;
            margin: auto;
        }

        /* Next & previous buttons */
        .prev, .next {
            cursor: pointer;
            position: absolute;
            top: 50%;
            width: auto;
            padding: 16px;
            margin-top: -22px;
            color: white;
            font-weight: bold;
            font-size: 18px;
            transition: 0.6s ease;
            border-radius: 0 3px 3px 0;
            user-select: none;
        }

        /* Position the "next button" to the right */
        .next {
            right: 0;
            border-radius: 3px 0 0 3px;
        }

            /* On hover, add a black background color with a little bit see-through */
            .prev:hover, .next:hover {
                background-color: rgba(0,0,0,0.8);
            }

        /* Caption text */
        .text {
            color: #f2f2f2;
            font-size: 15px;
            padding: 8px 12px;
            position: absolute;
            bottom: 8px;
            width: 100%;
            text-align: center;
        }

        /* Number text (1/3 etc) */
        .numbertext {
            color: #f2f2f2;
            font-size: 12px;
            padding: 8px 12px;
            position: absolute;
            top: 0;
        }

        /* The dots/bullets/indicators */
        .dot {
            cursor: pointer;
            height: 15px;
            width: 15px;
            margin: 0 2px;
            background-color: #bbb;
            border-radius: 50%;
            display: inline-block;
            transition: background-color 0.6s ease;
        }

            .active, .dot:hover {
                background-color: #717171;
            }

        /* Fading animation */
        .fade {
            -webkit-animation-name: fade;
            -webkit-animation-duration: 1.5s;
            animation-name: fade;
            animation-duration: 1.5s;
        }

        .webkit-keyframes fade {
            from

        {
            opacity: .4
        }

        to {
            opacity: 1
        }

        }

        .keyframes fade {
            from

        {
            opacity: .4
        }

        to {
            opacity: 1
        }

        }

        /* On smaller screens, decrease text size */
        .myImages only screen {
            .prev, .next, .text

        {
            "font-size: 11px"
        }
        }
    </style>
</head>
<body>
    <div class="welcome">
        <p class="w3-justify">
            Welcome to Fake Fest 2022, we have a line up of acts for you to enjoy and listen to during this week and a half festival.

            Our line up includes new, unknown, well known and underappreciated acts of varying styles of music.

            If you already have an idea of who you would like to see during your time at Fake Fest head on over to the band section and find them.
            If you are unsure of who you want to watch, want to see if your artist is playing or want to find a particular style of music, head over to the search tab for easier navigation.
        </p>

    </div>

    <div class="content">
        <p>
            <img src="" alt="Dan_Bull.jpg" id="myImages" />
        </p>
    </div>


    <section class=" w3-content" style="max-width:600px" style="text-align:center">
        <p class="w3-justify">

        </p>
    </section>
    <script src="~/js/site.js"></script>
</body>
